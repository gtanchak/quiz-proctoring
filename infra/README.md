# infra/

Infrastructure-as-code (**Terraform**) for the platform, organised per
environment (dev / staging / production).

Empty placeholder for now — provisioning is its own issue (`PRO-52`) and is not
part of the `PRO-46` skeleton. The target stack is AWS: ECS Fargate, RDS
(PostgreSQL), S3, CloudFront, SQS (see the Development Architecture doc, §3).
