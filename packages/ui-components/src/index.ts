/**
 * @proctoring/ui-components — shared React components for the two web apps.
 *
 * This is the PRO-46 skeleton. Real components (and the React dependency they
 * need) are added when the web apps grow UI worth sharing; for now this file
 * only proves the package resolves.
 */

export const UI_COMPONENTS_VERSION = "0.0.0" as const;
